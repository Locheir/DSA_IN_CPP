#include <iostream>
using namespace std;

int main() {
    // Standard Template Library in C++

    // It is a library (collection) of container classes, 
    // algorithm & iterators. It contains the implementation
    // of common data structures like vector, stacks, map, etc.

    return 0;
}