#include <iostream>
using namespace std;

int main() {
    // Binary Shift Operator : 

    // Left Shift : << 
    // a << b = a*(2**b)
    cout << (7<<2) << endl;

    // Right Shift : >>
    // a >> b = a/(2**b)
    cout << (7>>2) << endl;
    return 0;
}