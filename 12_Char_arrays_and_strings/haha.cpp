#include <iostream>
using namespace std;

int main() {
    int nums[] = {2, 3, 1, 1, 4};
    int n = sizeof(nums)/sizeof(int);

    int j=0;
    int count=0;
    while(j!=n-1){
        for (int i=j+1; i<=nums[j]; i++) {
            
        }
    }

    return 0;
}